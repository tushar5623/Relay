const fs = require('fs'); const csv = require('csv-parse/sync'); fs.writeFileSync('test.csv', 'name,role,contact,cost,status\r\nTest Catering,catering,test@example.com,8500,confirmed\r\nTest DJ,music,dj@example.com,1200,confirmed'); const data = fs.readFileSync('test.csv'); const records = csv.parse(data, { columns: true, skip_empty_lines: true, trim: true }); console.log(records);
