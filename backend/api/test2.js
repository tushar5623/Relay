const csv = require('csv-parse/sync'); const { v4: uuidv4 } = require('uuid'); const records = csv.parse('name,role,contact,cost,status\r\nTest Catering,catering,test@example.com,8500,confirmed\r\nTest DJ,music,dj@example.com,1200,confirmed', { columns: true, skip_empty_lines: true, trim: true }); const vendorsToInsert = records.map(r => ({ category: r.category || 'unknown', name: r.name || 'Unnamed Vendor', status: r.status || 'pending', quote: parseFloat(r.quote) || 0 })); console.log(vendorsToInsert);
